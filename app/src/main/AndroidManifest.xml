<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Petopia"
        tools:targetApi="31">

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="${NAVERMAP_CLIENT_ID}" />

        <!-- LoginActivity를 Launcher로 설정 -->
        <activity android:name="edu.sswu.petopia.ui.login.LoginActivity"
            android:exported="true">  <!-- android:exported 속성 추가 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity 선언 -->
        <activity android:name="edu.sswu.petopia.register.RegisterActivity"
            android:exported="false"
            android:label="RegisterActivity" />
        <!-- MainActivity 선언 -->
        <activity
            android:name="edu.sswu.petopia.MainActivity"
            android:exported="true" />

        <!-- EditUserActivity 선언 -->
        <activity android:name="edu.sswu.petopia.ui.profile.EditUserActivity" />
        <!-- EditPetActivity 선언 -->
        <activity android:name="edu.sswu.petopia.ui.profile.EditPetActivity" />

    </application>

</manifest>